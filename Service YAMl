apiVersion: v1
kind: Service
metadata:
  name: devops-prt-service
spec:
  type: NodePort
  selector:
    app: devops-prt
  ports:
    - port: 80           # Internal pod port
      targetPort: 80     # Container port
      nodePort: 30080    # External port to access from nodes
